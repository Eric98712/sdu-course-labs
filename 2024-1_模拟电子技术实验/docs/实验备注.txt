误差分析：在测量共模信号时，我们发现双端输出的信号仍然有波形，且幅值为56mV，比单端输出信号的幅值还大（此时带3k的负载，负载约为9.76mV）,
 
 
 后来，经网上查询，我们发现，可能的原因是我们使用的信号线并没有差分探头，因此，我们利用了示波器的Math函数，使两个波形相减，得到了如此实际的波形，结果与实际大概相符，但实际我们发现共模输出仍然有波形 ，这是因为两边的晶体管并不完全对称。
